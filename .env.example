# Supabase：兩人共享（多租戶 Workspace）用。沒填的話 App 仍可離線使用，
# 只是不會同步——畫面上的「兩人共享」卡片會顯示尚未設定。
#
# 1. 在 https://supabase.com 建立專案
# 2. Project Settings → API 取得下面兩個值
# 3. SQL Editor 執行 supabase/schema.sql
# 4. Authentication → Providers → 開啟 Anonymous sign-ins
#
# NEXT_PUBLIC_ 的值會在 build 時被寫進 bundle，所以改完要重新 build / 重啟 dev server。
#
# 這兩個值都可以公開（anon key 受 RLS 保護），所以用 NEXT_PUBLIC_ 前綴。
# 真正的解密金鑰由使用者的共用密碼在瀏覽器推導，不會出現在任何環境變數裡。
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
